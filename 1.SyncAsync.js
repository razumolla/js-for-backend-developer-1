// class- 1.1

// const colors = require("colors")

//? START

console.log("Welcome to acc");

// setTimeout(() => console.log("I am exciting to teach"), 2000);
setTimeout(() => {
    const user = { id: 1 };
    setTimeout(() => {
        const products = [{}, {}, {}, {}]
        setTimeout(() => {
            const price = [];
        }, 1000)
    }, 500)
}, 2000);

console.log("Hope you are also excited");

// ei vabe async use na kore promise use kora hoi